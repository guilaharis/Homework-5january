function mathFunction(num1, num2, num3, num4, num5) {
    minNum = Math.min(num1, num2, num3, num4, num5)
    maxNum = Math.max(num1, num2, num3, num4, num5)
    console.log("Min number is " + minNum + " and max number is " + maxNum)
}

mathFunction(2, 5, 7, 9, 0)


